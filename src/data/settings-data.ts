import { ref } from 'vue'

export const SETTINGS = {
    masterVolume: ref(45),
    musicVolume: ref(45),
    gameplayVolume: ref(45),
    effectsVolume: ref(45),
    selectedLanguage: ref('English'),
}